apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Bare-metal InfiniBand overlay
# Includes all steps except SR-IOV operator and VF config (IB uses RDMA shared devices)

namespace: openshift-gitops

resources:
  - ../../apps/01-operators-nfd-gpu
  - ../../apps/02-nfd-operands
  - ../../apps/11-ib-interface-normalization
  - ../../apps/13-nic-discovery
  - ../../apps/14-nvidia-network-operator
  - ../../apps/20-operators-gpu-readiness
  - ../../apps/21-gpu-operands
